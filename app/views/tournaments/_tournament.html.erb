<div id="<%= dom_id tournament %>">
<% tournament.poules.each_with_index do | poule, index | %>
    <h5>Poule <%= index+1 %> </h5>
    <% poule.fencers.uniq.each do | fencer | %>
        <p><%= fencer.name %></p>
    <% end %>
    <%= link_to "Show this poule", poule %>
<% end %>
</div>
