<%= simple_form_for(fencer) do |form| %>
  <% if fencer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fencer.errors.count, "error") %> prohibited this fencer from being saved:</h2>

      <ul>
        <% fencer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :points, style: "display: block" %>
    <%= form.text_field :points %>
  </div>

  <div>
    <%= form.input :photo, as: :file %>
  </div>

  

  <div>
    <%= form.button :submit, class: "btn btn-success" %>
  </div>
<% end %>
